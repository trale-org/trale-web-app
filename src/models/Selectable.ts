export interface Selectable {
  _id?: string;
  descriptive?: string; // discriptive string handle
  selected: boolean;
  headline: string;
  subHeadline: string;
  description: string;
  recommended: boolean;
}
